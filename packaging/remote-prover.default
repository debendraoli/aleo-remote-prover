# Bind address for HTTP server
#PROVER_LISTEN_ADDR=0.0.0.0:3030

# Max concurrent proving tasks
#MAX_CONCURRENT_PROOFS=4

# Enforce program editions once network requires it
#ENFORCE_PROGRAM_EDITIONS=true

# Override REST endpoint used for state queries
#REST_ENDPOINT_OVERRIDE=

# HOME directory for prover data (snarkvm parameters, keys, etc.)
HOME=/var/lib/remote-prover

# Log level (error, warn, info, debug, trace)
#RUST_LOG=info